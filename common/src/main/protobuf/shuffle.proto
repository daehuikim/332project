syntax = "proto3";

package shuffle;

service ShuffleNetwork {
    rpc SendPartition(SendPartitionRequest) returns(SendPartitionReply){}
}

message sAddress {
    string ip = 1;
    int32 port = 2;
}

enum sResultType {
    FAILURE = 0;
    PENDING = 1;
    SUCCESS = 2;
  }

message SendPartitionRequest {
    sAddress from = 1;
    sAddress to = 2;
    string partition = 3; /*TBD*/
}

message SendPartitionReply {
    sResultType result = 1;
}
